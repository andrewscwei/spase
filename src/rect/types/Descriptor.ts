/**
 * A type that can be used to create a {@link Rect}.
 */
export type Descriptor = Readonly<{
  /**
   * The `x` value.
   */
  x: number

  /**
   * The `y` value.
   */
  y: number

  /**
   * The `width` value.
   */
  width: number

  /**
   * The `height` value.
   */
  height: number
}>
